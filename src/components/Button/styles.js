import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container:{
        width: '100%',
        height: 30,
        marginVertical: 10,
        borderRadius:8
    },
    title:{
        flex: 1,
        fontSize: 30
    }
})
